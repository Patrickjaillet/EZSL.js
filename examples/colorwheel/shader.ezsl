center = uv - [0.5, 0.5]
angle = atan(center.y, center.x)
hueR = 0.5 + 0.5 * cos(angle)
hueG = 0.5 + 0.5 * cos(angle - 2.094)
hueB = 0.5 + 0.5 * cos(angle - 4.188)
d = length(center)
mask = 1.0 - smoothstep(0.45, 0.47, d)
color = [hueR * mask, hueG * mask, hueB * mask]
