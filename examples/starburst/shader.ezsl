center = uv - [0.5, 0.5]
angle = atan(center.y, center.x)
d = length(center)
spikes = 0.5 + 0.5 * cos(angle * 12.0)
threshold = 0.1 + spikes * 0.2
ray = 1.0 - smoothstep(threshold - 0.01, threshold, d)
color = [ray, ray * 0.7, 0.0]
