p = (uv - [0.5, 0.55]) * 3.0
px = p.x
py = -p.y
x2 = px * px
y2 = py * py
a = x2 + y2 - 1.0
lhs = a * a * a
rhs = x2 * py * py * py
implicit = lhs - rhs
inside = smoothstep(0.05, -0.05, implicit)
color = [inside, inside * 0.15, inside * 0.25]
